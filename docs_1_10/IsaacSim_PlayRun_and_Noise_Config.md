# IsaacSim 4.5 运行指南 + 噪声配置验证

**文档位置**: docs_1_10  
**更新日期**: 2026-01-10  
**主题**: Play运行、日志位置、交互控制、噪声配置

---

## 🎮 **第一部分: IsaacSim 4.5中训练完后的运行**

### 1️⃣ **日志输出位置**

#### 训练日志
```
项目根目录/logs/rsl_rl/[experiment_name]/[TIMESTAMP]_[run_name]/
├─ summary.csv              # 汇总数据
├─ tb_record/               # TensorBoard记录
│  ├─ events.out.tfevents.* 
│  └─ (TensorBoard使用这个)
├─ config.py                # 环境配置快照
├─ model_*.pt               # 检查点模型
│  ├─ model_500.pt
│  ├─ model_1000.pt
│  ├─ model_1500.pt
│  ├─ model_2000.pt
│  ├─ model_2500.pt
│  └─ model_3000.pt         # 最终模型 (通常用这个)
└─ logs/                    # 标准输出日志
   └─ *.log
```

#### 具体路径示例 (Task 2+3)
```
logs/rsl_rl/pf_task2_3_flat/2026-01-10_14-45-30_Task2-3_Corrected_Weights_v1/
├─ config.py
├─ model_3000.pt
├─ summary.csv
└─ tb_record/
```

#### Play运行日志
```
项目根目录/logs/rsl_rl/[experiment_name]/[TIMESTAMP]_[run_name]/logs/
├─ play_output_[时间戳].log
├─ policy_[时间戳].txt
└─ (包含实时的速度、奖励、action等信息)
```

---

### 2️⃣ **Play运行后的日志内容**

#### 找到日志文件
```bash
# 训练完成后，查看最新的日志目录
ls -ltr logs/rsl_rl/pf_task2_3_flat/

# 进入该目录
cd logs/rsl_rl/pf_task2_3_flat/2026-01-10_14-45-30_Task2-3_Corrected_Weights_v1/

# 查看Play输出
cat logs/play_output_*.log | tail -100  # 查看最后100行
```

#### 日志中包含的关键信息

```
┌─ Play启动信息 ────────────────────────────────────────┐
│ [INFO]: Loading checkpoint from: model_3000.pt        │
│ [INFO]: Policy initialized successfully              │
│ [INFO]: Env configured: Isaac-Limx-PF-Task2-3-Play-v0 │
└───────────────────────────────────────────────────────┘

每个episode:
├─ Episode: 1/100
├─ Step: 1-1000
│  ├─ Base Velocity: [vx, vy, vz]
│  ├─ Base Ang Vel: [wx, wy, wz]
│  ├─ Action: [a1, a2, a3, a4, a5, a6]
│  ├─ Reward: 0.45
│  ├─ Terminal: False
│  └─ Crash: No
├─ Episode Summary:
│  ├─ Total Reward: 62.1
│  ├─ Episode Length: 956
│  └─ Status: Success
└─ Next episode...

最后统计:
├─ Average Reward: 61.8
├─ Average Episode Length: 952
├─ Crash Rate: 0.08
└─ [INFO]: Play finished successfully
```

---

### 3️⃣ **IsaacSim控制面板的交互功能**

#### 📺 **主要可交互面板**

```
Isaac Lab主窗口布局:
┌──────────────────────────────────────────────────────┐
│ ≡ Menu Bar (菜单栏)                                   │
├──────────────────────────────────────────────────────┤
│ [>Play] [⏸ Pause] [⏹ Stop] [↻Reset] [Speed: 1x] [∴∴∴] │ ← 控制栏
├──────────────────────────────────────────────────────┤
│                                                      │
│          3D Viewport (3D视图)                        │
│          └─ 显示机器人和环境                        │
│                                                      │
├──────────────────────────────────────────────────────┤
│ Inspector Panel (属性面板)  │ Property Panel (参数面板) │
│ └─ 选中物体的属性          │ └─ 场景参数调整         │
└──────────────────────────────────────────────────────┘
```

#### 🎛️ **常用交互按钮**

| 按钮 | 功能 | 快捷键 | 说明 |
|------|------|--------|------|
| ▶️ Play | 开始/继续运行 | Space | 播放simulation |
| ⏸ Pause | 暂停运行 | Space | 暂停当前运行 |
| ⏹ Stop | 停止运行 | Esc | 完全停止(重置) |
| ↻ Reset | 重置场景 | R | 恢复初始状态 |
| 🔍 Zoom | 缩放视图 | 滚轮 | 放大/缩小 |
| 🔄 Rotate | 旋转视图 | 中键+拖 | 三维旋转 |
| 👁 Camera | 相机选择 | C | 切换多个视角 |
| ⚙ Settings | 设置 | ... | 调整物理引擎参数 |

#### ⚙️ **Settings中可调的参数**

```
Physics Settings:
├─ Time Step: 0.005s (5ms) ← 通常不改
├─ Gravity: [0, 0, -9.81] m/s²
├─ Solver Type: GPU (CUDA) / CPU
├─ Iterations: 4 (精度参数)
└─ Enable CCD: ✓ (连续碰撞检测)

Rendering Settings:
├─ Resolution: 1920x1080
├─ FPS: 60
├─ Antialiasing: MSAA
└─ Debug Visualization: On/Off

Logging Settings:
├─ Log Level: INFO / DEBUG / WARNING
├─ Log File: logs/isaac_sim.log
└─ Print to Console: ✓
```

---

#### 📊 **实时监控窗口**

Play运行时可以看到的实时信息：

```
左下角信息面板 (通常显示):
┌─────────────────────────────────┐
│ FPS: 120 (GPU加速时)            │
│ Iteration: 1345/3000            │
│ Time: 13:45:30                  │
│ Camera FPS: 60                  │
│ Physics FPS: 200                │
└─────────────────────────────────┘

右侧性能指标:
┌────────────────────────────────┐
│ Compute Time: 3.2ms            │
│ Physics Time: 1.8ms            │
│ Rendering Time: 1.1ms          │
│ Total Frame Time: 6.1ms        │
└────────────────────────────────┘
```

---

### 4️⃣ **使用Play时的常用命令**

#### 启动Play
```bash
# 基础Play命令
python scripts/rsl_rl/play.py \
  --task Isaac-Limx-PF-Task2-3-Play-v0 \
  --num_envs 32 \
  --load_run 2026-01-10_14-45-30_Task2-3_Corrected_Weights_v1 \
  --checkpoint model_3000.pt
```

#### 常用Play标志
```bash
# 带GUI运行 (默认)
--headless false  # 显示可视化窗口

# 无头运行 (快速)
--headless true   # 不显示窗口，输出结果到日志

# 指定GPU
--device cuda:0   # 使用GPU 0

# 指定环境数
--num_envs 64     # 并行运行64个环境

# 长时间运行
--max_runs 1000   # 运行1000个episodes
```

---

## 🔴 **第二部分: 噪声配置验证**

### ⚠️ **重要发现: Play中的噪声问题！**

我查证了代码，有一个**需要修复的问题**。

#### 当前状态

```python
# limx_pointfoot_env_cfg.py, 行 657-665

@configclass
class PFTask2And3EnvCfg_PLAY(PFTask2And3EnvCfg):
    """Play version of Task 2+3 - disable observation corruption."""
    def __post_init__(self):
        super().__post_init__()
        self.scene.num_envs = 32
        # ❌ 禁用观测噪声，其他配置保持与训练一致
        self.observations.policy.enable_corruption = False  # ← 这一行！
```

#### 噪声的具体含义

在 `limx_base_env_cfg.py` 中定义了：

```python
# 训练时添加的噪声 (enable_corruption=True时)
base_lin_vel = ObsTerm(
    func=mdp.base_lin_vel,
    noise=GaussianNoise(mean=0.0, std=0.05),  # ± 5% 高斯噪声！
    ...
)

proj_gravity = ObsTerm(
    func=mdp.projected_gravity,
    noise=GaussianNoise(mean=0.0, std=0.025), # ± 2.5%
    ...
)

joint_pos = ObsTerm(
    func=mdp.joint_pos_rel,
    noise=GaussianNoise(mean=0.0, std=0.01),  # ± 1%
    ...
)

joint_vel = ObsTerm(
    func=mdp.joint_vel,
    noise=GaussianNoise(mean=0.0, std=0.01),  # ± 1%
    ...
)
```

---

### 📊 **Train vs Play 噪声对比**

| 阶段 | 观测噪声 | 状态 | 影响 |
|------|---------|------|------|
| **Training** | ✅ 启用 | enable_corruption=True | 模型学到"鲁棒性" |
| **Play** | ❌ 禁用 | enable_corruption=False | ❌ **Domain Mismatch!** |

#### 为什么这是个问题？

```
训练阶段:
  ├─ 模型看到: vx=1.0±0.05 (噪声干扰)
  ├─ 模型学到: "即使观测有噪声，也要稳定行动"
  └─ 结果: 学到的策略"对噪声鲁棒"

Play阶段:
  ├─ 模型看到: vx=1.0 (完美观测，无噪声！)
  ├─ 但模型的权重是基于"有噪声"的环境训练的
  └─ 结果: 模型行为可能不稳定！

这正是我之前说的"观测噪声domain mismatch"的原因！
```

---

### ✅ **建议修复**

#### 选项 A: 保持Play无噪声(当前)
```python
# 保持原样
self.observations.policy.enable_corruption = False

# 理由:
# - 更"干净"的推理
# - 模型知道自己在做什么
# - 适合演示和测试
```

#### 选项 B: 启用Play噪声(推荐改进) ⭐
```python
# 修改为:
self.observations.policy.enable_corruption = True

# 理由:
# - 与训练条件一致
# - 模型行为更稳定
# - 避免domain mismatch
# - Crash率应该下降
```

#### 选项 C: 减弱Play噪声(折中)
```python
# 不修改enable_corruption，但调整std参数
# 例如在Play特定的观测中:
noise=GaussianNoise(mean=0.0, std=0.01)  # 从0.05降到0.01

# 理由:
# - 保留部分鲁棒性
# - 但不完全相同
# - 介于A和B之间
```

---

### 📝 **噪声的详细数值**

#### 训练中使用的噪声 (enable_corruption=True)

```python
# 来自 limx_base_env_cfg.py

观测项              噪声std      含义
─────────────────────────────────────────
base_lin_vel       0.05  (5%)   速度添加5%噪声
base_ang_vel       0.05  (5%)   角速度添加5%噪声
proj_gravity       0.025 (2.5%) 重力投影2.5%噪声
joint_pos          0.01  (1%)   关节位置1%噪声
joint_vel          0.01  (1%)   关节速度1%噪声
last_action        0.01  (1%)   上一个动作1%噪声
velocity_error     0.01  (1%)   速度误差1%噪声
```

#### Play中的设置 (当前)

```python
enable_corruption = False  # ← 全部禁用
```

---

### 🔍 **如何验证是否应该加入噪声**

#### 方法1: 对比Play输出
```bash
# 当前的Play (无噪声)
python scripts/rsl_rl/play.py \
  --task Isaac-Limx-PF-Task2-3-Play-v0 \
  --checkpoint model_3000.pt \
  > current_play.log

# 修改PFTask2And3EnvCfg_PLAY, 改为 enable_corruption=True
# 再运行一次
python scripts/rsl_rl/play.py \
  --task Isaac-Limx-PF-Task2-3-Play-v0 \
  --checkpoint model_3000.pt \
  > play_with_noise.log

# 对比指标:
# - Crash rate 是否下降?
# - Reward是否更稳定?
# - Episode length是否更长?
```

#### 方法2: 查看TensorBoard
```bash
# 启用TensorBoard比较两次运行
tensorboard --logdir=logs/rsl_rl/pf_task2_3_flat/
```

---

## 📋 **快速参考表**

### 日志路径速查

```
┌─ 类型                          ┌─ 路径
├─ 训练输出                      ├─ logs/rsl_rl/pf_task2_3_flat/[TIME]_[NAME]/
├─ 模型检查点                    ├─ logs/rsl_rl/pf_task2_3_flat/[TIME]_[NAME]/model_*.pt
├─ TensorBoard数据               ├─ logs/rsl_rl/pf_task2_3_flat/[TIME]_[NAME]/tb_record/
├─ Play输出日志                  ├─ logs/rsl_rl/pf_task2_3_flat/[TIME]_[NAME]/logs/
├─ IsaacSim系统日志              ├─ ~/.local/share/ov/pkg/Isaac-Sim-*/logs/
└─ Python stdout/stderr          └─ 终端或重定向的文件
```

### 噪声配置速查

```
配置项                          值          含义
─────────────────────────────────────────────────────
enable_corruption (Train)      True        启用观测噪声
enable_corruption (Play)       False       ❌ 禁用(可能需要改)
base_lin_vel 噪声              0.05 (5%)   相对较大
proj_gravity 噪声              0.025       中等
joint_pos/vel 噪声             0.01 (1%)   较小
```

---

## 🎯 **我的建议**

1. **现在**: 继续训练到3000迭代
2. **训练完成后**: 
   - 先用当前无噪声Play测试一次
   - 记录Reward和crash率
3. **然后** (可选优化):
   - 修改 `PFTask2And3EnvCfg_PLAY` 中的 `enable_corruption = True`
   - 再运行一次Play
   - 对比两次结果
   - 如果有改善(crash↓, reward稳定↑), 保留这个修改

---

**现在你对IsaacSim的运行和日志有了完整理解！** 🚀
