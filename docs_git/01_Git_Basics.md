# Git 完整入门指南

**目标**: 理解你现在面临的 Git 问题和完整的 Git 工作流

---

## 📊 你现在面临的问题：身份混乱

```
┌─────────────────────────────────────────────┐
│ 你的情况                                     │
├─────────────────────────────────────────────┤
│ GitHub 账号（仓库所有者）  : Limozknight   │
│ GitHub 账号（本地配置）    : knightkk4      │ ← 不匹配！
│                                             │
│ 结果：推送时权限拒绝                         │
│      "Permission denied to knightkk4"       │
└─────────────────────────────────────────────┘
```

---

## 🔄 Git 工作流程图解

### 最简化版（3 个核心概念）

```
┌──────────────────────────────────────────────────────────┐
│                 你的电脑（本地）                          │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Working Directory                               │   │
│  │ (你修改的代码文件)                               │   │
│  │                                                 │   │
│  │  file.py, config.yaml, README.md               │   │
│  └──────────────┬──────────────────────────────────┘   │
│                 │ git add .                             │
│                 ↓                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Staging Area (Index)                            │   │
│  │ (准备提交的文件快照)                             │   │
│  │                                                 │   │
│  │  file.py ✅, config.yaml ✅                     │   │
│  └──────────────┬──────────────────────────────────┘   │
│                 │ git commit -m "message"               │
│                 ↓                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Local Repository                                │   │
│  │ (本地提交历史)                                   │   │
│  │                                                 │   │
│  │ commit 1: Initial commit                       │   │
│  │ commit 2: Add features                         │   │
│  │ commit 3: Fix bugs      ← HEAD（当前位置）      │   │
│  └──────────────┬──────────────────────────────────┘   │
│                 │ git push                              │
│                 ↓ (需要认证)                             │
└──────────────────────────────────────────────────────────┘
                  │
                  │ 网络
                  ↓
┌──────────────────────────────────────────────────────────┐
│              GitHub（远程仓库）                           │
│                                                          │
│  github.com/Limozknight/limxtron1lab-training           │
│                                                          │
│  commit 1: Initial commit                              │
│  commit 2: Add features                                │
│  commit 3: Fix bugs      ← origin/master（远程位置）    │
└──────────────────────────────────────────────────────────┘
```

### 详细版（7 个步骤的完整流程）

```
第 1 步：修改代码（在 Working Directory）
   你编辑文件：limx_base_env_cfg.py
   │
   ├─ 修改奖励权重
   ├─ 修改观测噪声
   └─ 保存文件
   
   状态：文件已修改，但 Git 还不知道

═══════════════════════════════════════════════

第 2 步：查看修改（git status）
   $ git status
   
   输出：
   On branch master
   Changes not staged for commit:
     (use "git add <file>..." to stage for commit)
           modified:   limx_base_env_cfg.py
   
   说明：告诉你有文件被修改了

═══════════════════════════════════════════════

第 3 步：添加到暂存区（git add）
   $ git add limx_base_env_cfg.py
   
   或者添加所有修改：
   $ git add .
   
   状态：文件已加入 Staging Area，等待提交

═══════════════════════════════════════════════

第 4 步：查看准备提交的内容（git diff --staged）
   $ git diff --staged
   
   输出：你看到具体的修改内容（绿色 + 号）

═══════════════════════════════════════════════

第 5 步：提交到本地仓库（git commit）
   $ git commit -m "Increase reward weights for Task 2.2"
   
   输出：
   [master abc1234] Increase reward weights for Task 2.2
    1 file changed, 5 insertions(+), 3 deletions(-)
   
   说明：创建了一个新的 commit，本地保存

═══════════════════════════════════════════════

第 6 步：查看提交历史（git log）
   $ git log --oneline
   
   输出：
   abc1234 (HEAD -> master) Increase reward weights
   def5678 Add GitHub setup guide
   ghi9101 Initial commit
   
   说明：你的所有提交记录

═══════════════════════════════════════════════

第 7 步：推送到 GitHub（git push）
   $ git push origin master
   
   需要验证身份！
   ├─ 使用 Token
   ├─ 使用 SSH Key
   └─ 使用用户名+密码（已弃用）
   
   输出：
   Enumerating objects: 3, done.
   Writing objects: 100% (3/3), ...
   To github.com:Limozknight/limxtron1lab-training.git
    * [new branch]      master -> master
   
   说明：代码已上传到 GitHub
```

---

## 🔐 核心概念：Git 的三个位置

```
┌────────────────┐
│  Working Dir   │  ← 你正在编辑的文件（硬盘上的真实文件）
└────────────────┘
        │
        │ git add      （"我想保存这些修改"）
        ↓
┌────────────────┐
│ Staging Area   │  ← 准备提交的文件快照（临时保存）
└────────────────┘
        │
        │ git commit   （"我确认这个提交"）
        ↓
┌────────────────┐
│   Local Repo   │  ← 本地版本历史（你的电脑上）
└────────────────┘
        │
        │ git push     （"上传到网络"）
        ↓
┌────────────────┐
│ Remote Repo    │  ← GitHub 上的仓库（网络上）
│  (origin)      │
└────────────────┘
```

**类比**：
- Working Directory = 你的草稿纸
- Staging Area = 你的草稿夹
- Local Repo = 你的笔记本
- Remote Repo = 图书馆

---

## 📝 常用 Git 命令详解

### 命令 1：查看状态
```bash
git status
```
**作用**: 看看有什么文件被修改了

**输出例子**：
```
On branch master
Changes not staged for commit:
  modified:   file1.py
  modified:   file2.yaml

Untracked files:
  new_file.txt
```

**解读**:
- `modified`: 文件被改了，但还没 add
- `Untracked`: 全新文件，Git 还不知道

---

### 命令 2：添加文件
```bash
git add file.py         # 添加单个文件
git add .               # 添加所有修改
git add *.py            # 添加所有 .py 文件
```

**作用**: 将文件放入 Staging Area（准备提交）

**提示**: 这步之后，还没正式保存，可以继续 add 其他文件

---

### 命令 3：查看修改内容
```bash
git diff                # 看 Working Dir vs Staging Area 的区别
git diff --staged       # 看 Staging Area vs Local Repo 的区别
```

**作用**: 确认你改了什么

**输出例子**：
```diff
diff --git a/file.py b/file.py
index 1234567..abcdefg 100644
--- a/file.py
+++ b/file.py
@@ -10,5 +10,7 @@
  old_line = 5
-new_line = 10           ← 删除（红色）
+new_line = 15           ← 添加（绿色）
+extra_line = 20         ← 添加（绿色）
```

---

### 命令 4：提交
```bash
git commit -m "写一个清楚的提交信息"
```

**作用**: 将 Staging Area 的文件保存到 Local Repo

**好的提交信息例子**:
```
✅ "Fix reward weights for Task 2.2"
✅ "Add domain randomization events"
✅ "Update terrain configuration"

❌ "update"（太模糊）
❌ "asdf"（没意义）
```

---

### 命令 5：查看提交历史
```bash
git log                 # 完整提交历史
git log --oneline       # 简洁版本
git log -5              # 最近 5 个提交
git log --graph         # 显示分支树
```

**输出例子**：
```
abc1234 (HEAD -> master) Fix reward weights
def5678 Add GitHub setup guide
ghi9101 Update documentation
jkl1213 Initial commit
```

**解读**:
- `abc1234`: 提交的短 ID（可用于回溯）
- `(HEAD -> master)`: 你现在在这里
- 后面是提交信息

---

### 命令 6：配置身份
```bash
# 全局配置（所有项目）
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

# 仅当前项目配置
git config user.name "Your Name"
git config user.email "your@email.com"

# 查看配置
git config --list
```

**为什么需要**：提交时需要知道是谁提交的

---

### 命令 7：配置远程仓库
```bash
git remote -v                          # 查看远程配置
git remote add origin <URL>            # 添加远程
git remote set-url origin <新URL>      # 修改远程
git remote remove origin               # 删除远程
```

**origin 是什么**：
- `origin` = GitHub 上的仓库的昵称
- 你可以有多个远程：`origin`, `upstream` 等

---

### 命令 8：推送到 GitHub
```bash
git push origin master                 # 推送 master 分支
git push -u origin master              # 推送并设置上游分支
git push                               # 推送（如果已设置上游）
```

**`-u` 参数的作用**：
- 只需要用一次
- 建立本地分支和远程分支的追踪关系
- 之后可以直接用 `git push` 不用指定

---

## 🔄 工作流程步骤总结

### 常见场景 1：修改了一个文件并推送

```bash
# 1. 修改文件（在编辑器中）
# vim limx_base_env_cfg.py

# 2. 检查修改
git status

# 3. 添加修改
git add limx_base_env_cfg.py

# 4. 提交
git commit -m "Update reward weights"

# 5. 推送
git push
```

---

### 常见场景 2：同时修改了多个文件

```bash
# 1. 修改多个文件
# ... 在编辑器中修改 ...

# 2. 查看都修改了什么
git status

# 3. 分别查看每个文件的修改
git diff file1.py
git diff file2.yaml

# 4. 添加所有修改（一步到位）
git add .

# 5. 再看一遍准备提交的内容
git diff --staged

# 6. 提交
git commit -m "Update configurations and add features"

# 7. 推送
git push
```

---

### 常见场景 3：发现改错了（还没提交）

```bash
# 情况 1: 改错了，还没 add
git checkout file.py        # 恢复文件到上次提交的状态

# 情况 2: 改错了，已经 add 了
git reset HEAD file.py      # 取消 add
git checkout file.py        # 恢复文件

# 情况 3: 已经 commit 了，想撤销
git revert <commit_id>      # 创建一个新提交来撤销
# 或
git reset --hard HEAD~1     # 删除最后一个提交（危险！）
```

---

## 🔑 认证问题详解

### 你现在的问题：

```
GitHub 账号（仓库所有者）：Limozknight
本地 Git 配置：knightkk4
结果：Permission Denied
```

### 为什么会这样：

```
推送时：
  git push
    ↓
  系统检查："谁在推送？"
    ↓
  检查本地配置：user.name = knightkk4
    ↓
  验证身份：使用 knightkk4 的 token / SSH key
    ↓
  GitHub 检查："knightkk4 有权限访问 Limozknight 的仓库吗？"
    ↓
  答案：No → Permission Denied
```

### 解决方案：

**选项 A：修改本地 Git 配置**（最简单）
```bash
git config --global user.name "Limozknight"
git config --global user.email "你的邮箱"
```

**选项 B：使用专用配置**（针对这个项目）
```bash
cd 项目目录
git config user.name "Limozknight"
git config user.email "你的邮箱"
```

---

## 📚 完整的一次工作流程（真实例子）

### 目标：完成任务 2.2 并推送到 GitHub

```bash
# ===== 第 1 天：修改代码 =====

# 1. 进入项目目录
cd c:\Users\17950\Desktop\everything\IE\SDM5008\limxtron1lab-main\ \(1\)\limxtron1lab-main

# 2. 修改文件（在 VS Code 或任何编辑器中）
# 编辑：exts/bipedal_locomotion/bipedal_locomotion/tasks/locomotion/cfg/PF/limx_base_env_cfg.py
# 修改：奖励权重从 1.0 改为 2.0

# 3. 查看修改
git status
# 输出：modified: exts/bipedal_locomotion/.../limx_base_env_cfg.py

# 4. 详细查看修改内容
git diff exts/bipedal_locomotion/bipedal_locomotion/tasks/locomotion/cfg/PF/limx_base_env_cfg.py
# 输出：显示 weight=1.0 改为 weight=2.0

# ===== 第 2 天：训练完成后提交 =====

# 5. 训练完成，现在推送代码
git add .

# 6. 再次确认要提交什么
git diff --staged | head -20

# 7. 提交
git commit -m "Task 2.2: Increase reward weights for better convergence"

# 8. 查看提交历史
git log --oneline -3

# 9. 推送到 GitHub
git push
# 需要输入 token 或使用 SSH

# 10. 验证成功
# 打开 https://github.com/Limozknight/limxtron1lab-training
# 应该能看到新的提交
```

---

## ✅ 一句话总结每个命令的目的

```
git add         = "我确认这个文件要提交"
git commit      = "我确认这个版本"
git push        = "我要上传到 GitHub"
git pull        = "我要从 GitHub 下载最新代码"
git status      = "现在状态怎么样？"
git log         = "之前都改过什么？"
git diff        = "具体改了什么？"
git config      = "设置 Git 身份"
```

---

